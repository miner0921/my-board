-- =====================================================
-- 013. invoice_reopens.reason NULL 허용
-- =====================================================
-- 로컬 개발: pgAdmin에서 boarddb 선택 후 Query Tool에서 실행
-- 배포 시: Neon SQL Editor에서 동일하게 실행
--
-- 재개 사유를 선택사항으로 완화. 작업 현장에서 시간 없을 때
-- 빠르게 재개 가능하도록.
--
-- 자동 재개(시스템)는 항상 'reason' 값을 채우므로(수량 추가로 자동 재개)
-- NULL 행은 "사용자가 사유 없이 수동 재개" 케이스로 식별 가능.
--
-- 재실행 안전.
-- =====================================================

ALTER TABLE invoice_reopens
  ALTER COLUMN reason DROP NOT NULL;
