-- =====================================================
-- 042. items.inspection_exempt (검수 제외 = "스캔불필요" 품목)
-- =====================================================
-- 로컬/배포: Cloud SQL(서울) - psql 또는 Cloud SQL Studio에서 실행
--
-- 의미:
--   체크된 품목의 송장 라인은 "고객 표기용"이다.
--     - 스캔/수동 챙김이 모두 불필요
--     - 진행률(N/M)/완료 판정에서 제외
--     - 검수 화면/송장 상세/전체 상품에 "스캔불필요" 배지로 표시만 (사라지진 않음)
--
-- [주의] 기존 scan_exempt(동봉, 018)와 완전히 별개다:
--     - scan_exempt(동봉)  = 안내 인쇄물 배지. 여전히 수동 챙김으로 확인해야 완료(검수 대상).
--     - inspection_exempt  = 검수 자체에서 빠짐(스캔/수동 챙김 불필요, 진행률/완료 제외).
--   두 플래그는 독립적으로 세팅될 수 있다.
--
-- 세팅 방법:
--   - 수동: 품목 등록/수정 모달의 "스캔불필요" 체크박스만. (자동 세팅 없음)
--
-- 기존 품목은 전부 FALSE(기본값). 재실행 안전(IF NOT EXISTS).
-- =====================================================

ALTER TABLE items
  ADD COLUMN IF NOT EXISTS inspection_exempt BOOLEAN NOT NULL DEFAULT FALSE;

-- =====================================================
-- [검증]
--   \d items                                            -- inspection_exempt 컬럼 확인
--   SELECT COUNT(*) FROM items WHERE inspection_exempt;  -- 처음엔 0건
-- =====================================================
